# 5.5 LangChain4j Persistent Memory Demo

对应课程 `5.5｜LangChain4j 持久化记忆和 RAG 边界`。

```bash
mvn -f ../pom.xml -pl langchain4j-persistent-memory-demo exec:java
```

示例演示 `ChatMemoryStore` 的读写接口，以及 RAG 检索内容默认不写入 ChatMemory 的边界。
